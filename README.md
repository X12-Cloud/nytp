# NyPkg (nytp)
- Package manager for the nytrogen ecosystem

## Build and run
- Build the project:
```bash
./run.sh
```

- Run the project
```bash
./build/nypkg -S pkg_name.json # use tree-sitter-nytrogen.json as a test
```

## Im too lazy to write more docs
